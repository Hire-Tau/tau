# Configuration

How a tau instance is configured: the `.env` file, the environment variables it
holds, and how humans and machines authenticate against the API.

`bun run setup` writes most of this for you — see
[docs/wiki/setup.md](setup.md#what-it-writes) for the keys it manages and when it
replaces them. This page is the reference for everything else.

## Config files

```bash
cp .env.example .env
cp ecosystem.config.example.js ecosystem.config.js
```

`.env` holds the instance configuration; `ecosystem.config.js` is the pm2
process file for `tau-api` and `tau-worker` (or `tau-<label>-api` /
`tau-<label>-worker` on a labelled instance — `bun run setup` generates it from
the example with those names substituted in). Both are gitignored.

**Provider API keys:** AI provider credentials (Anthropic, OpenAI, Google, etc.)
are managed from the web UI (**Settings > AI Providers**), or with
`tau provider-auth set <provider> <key>` for an API key (`list`, `get`, `delete`
and `oauth-providers` round out the command). OAuth logins for Claude Pro/Max,
ChatGPT Plus/Pro and GitHub Copilot subscriptions are web-UI only. This requires
`TAU_ENCRYPTION_KEY` — without it the page fails with
`Cannot mutate secrets: TAU_ENCRYPTION_KEY not configured`. Generate a key with
`echo "TAU_ENCRYPTION_KEY=$(openssl rand -hex 32)" >> .env`.

## Environment variables (`.env`)

| Variable                                   | Description                                                                                                                                                                                                                                                                                                                                                                                                                                          | Default                                           |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| `HOME_DIR`                                 | Root directory for Tau data (sessions, workspaces). A leading `~` is expanded (`~user/…` is not); a relative value stays relative to the working directory, so prefer an absolute path.                                                                                                                                                                                                                                                              | `$HOME/.tau`                                      |
| `DATABASE_URL`                             | PostgreSQL connection string                                                                                                                                                                                                                                                                                                                                                                                                                         | `postgres://postgres:postgres@localhost:5432/tau` |
| `TAU_ENCRYPTION_KEY`                       | 32-byte hex key for encrypting secrets in the database. Generate with `openssl rand -hex 32`. **Required** for Settings > AI Providers and Settings > Integrations; without it those pages fail with `Cannot mutate secrets: TAU_ENCRYPTION_KEY not configured`.                                                                                                                                                                                     | -                                                 |
| `PORT`                                     | API server port                                                                                                                                                                                                                                                                                                                                                                                                                                      | `3000`                                            |
| `WORKER_PORT`                              | Port the worker's own HTTP server listens on (health and internal routes). `bun run setup` writes `PORT + 2`.                                                                                                                                                                                                                                                                                                                                        | `3002`                                            |
| `TAU_WORKER_BIND`                          | Host the worker's stream server (`WORKER_PORT`) binds. Empty falls through to `HOST`; with neither set the worker binds loopback only (`127.0.0.1`), consistent with the other local services — remote access to `WORKER_PORT` requires `HOST` or `TAU_WORKER_BIND`. Kubernetes is auto-detected and binds all interfaces.                                                                                                                           | (loopback)                                        |
| `TAU_WORKER_EVENT_PORT`                    | Port for the worker's cross-process listener. It binds loopback by default; supported split-namespace deployments use `TAU_WORKER_EVENT_BIND` and peer URL overrides on a private network. `bun run setup` writes `PORT + 3`.                                                                                                                                                                                                                        | `3003`                                            |
| `TAU_INSTANCE`                             | Instance label of this checkout — it names the pm2 apps, the PostgreSQL container and volume, and the default `HOME_DIR`. Written by `bun run setup --instance <label>`; `tau server` commands also read it to pick the instance to act on. See [docs/wiki/setup.md](setup.md#multiple-instances).                                                                                                                                                   | `tau`                                             |
| `TAU_PM2_API_NAME` / `TAU_PM2_WORKER_NAME` | pm2 app names of this instance, used by the pm2 system-log provider and by the root `start:core` / `stop:core` / `reload:*` scripts (via `scripts/pm2-name.ts`). Setup derives them from `TAU_INSTANCE`.                                                                                                                                                                                                                                             | `tau-api` / `tau-worker`                          |
| `TAU_INTERNAL_EVENT_TOKEN`                 | Shared secret authenticating API↔worker events. Resolution is explicit token, then HMAC-derived from `TAU_ENCRYPTION_KEY`, then a random per-process fail-closed token.                                                                                                                                                                                                                                                                              | -                                                 |
| `MAX_CONCURRENT_AGENTS`                    | Max agent runs the worker executes in parallel — any whole number ≥ 1 (values that are not are ignored and the default applies). This is the instance **default**: the `MAX_CONCURRENT_AGENTS` entry under Settings → System → Agent Execution overrides it and takes effect without a restart; clear that setting to fall back here. That UI setting is additionally capped at 500; this variable is not, so an existing large value keeps working. | `30`                                              |
| `TAU_PASSWORD`                             | Bootstrap/legacy password, accepted only while no admin has a passkey. Set in deployment configuration; leaving it empty does not disable account authentication.                                                                                                                                                                                                                                                                                    | -                                                 |
| `TAU_WEB_ORIGIN`                           | Browser app **bare origin** (`scheme://host`, **no path**) for passkey/WebAuthn, the CORS allowlist, and session-cookie scope. If the app is served under a base path (e.g. `APP_URL=https://host/tau`), set this to `https://host` only — a path here breaks passkey registration. Falls back to `WEBAUTHN_ORIGIN` → `APP_URL` (path stripped).                                                                                                     | (from `APP_URL`)                                  |
| `WEBAUTHN_RP_ID`                           | WebAuthn relying-party ID — **bare registrable domain** (`host`, no protocol/port/path). Defaults to the host of `TAU_WEB_ORIGIN`; pin only to use a parent domain.                                                                                                                                                                                                                                                                                  | (host of web origin)                              |
| `WEBAUTHN_RP_NAME`                         | Name shown in the passkey dialog.                                                                                                                                                                                                                                                                                                                                                                                                                    | `Tau`                                             |
| `VAPID_SUBJECT`                            | `mailto:` or `https:` URL identifying the push sender. Required for iOS/Safari push notifications (Apple rejects `.local` domains).                                                                                                                                                                                                                                                                                                                  | `mailto:you@example.com`                          |
| `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY`   | Auto-generated and stored in SecretStore (database). No manual setup needed. Auto-migrates from `config/vapid.json` on first read.                                                                                                                                                                                                                                                                                                                   | (auto-managed)                                    |
| `OPENAI_API_KEY`                           | Legacy environment source for OpenAI Realtime voice, audio transcription, and memory embeddings. Prefer Settings → Integrations → OpenAI API services; environment keys also retain agent-model provider discovery behavior.                                                                                                                                                                                                                         | -                                                 |
| `GOOGLE_APPLICATION_CREDENTIALS`           | Optional server-side Application Default Credentials path for Google Text-to-Speech. Prefer the Google Cloud integration card for JSON credentials; its enable switch also gates ADC-based speech.                                                                                                                                                                                                                                                   | -                                                 |
| `TAU_SANDBOX_RUNTIME`                      | **Required, no default** — the core refuses to start without it. One of `docker-sysbox`, `docker-socket`, `k8s`, `vm`, `host`. See [sandbox-runtimes.md](sandbox-runtimes.md) to choose.                                                                                                                                                                                                                                                             | (required)                                        |
| `TAU_SANDBOX_IMAGE`                        | Docker image for sandbox containers/pods                                                                                                                                                                                                                                                                                                                                                                                                             | `tau-sandbox:latest`                              |
| `TAU_SANDBOX_MEMORY_LIMIT`                 | Memory limit for sandbox pods/containers. Defaults to `8Gi` in local k3d mode and `4Gi` otherwise.                                                                                                                                                                                                                                                                                                                                                   | `4Gi` / `8Gi` local                               |
| `TAU_SERVE_WEB`                            | Serve the built web UI from the core on `PORT`, so the app, `/api/*` and `/ws` share one origin. `1`/`true`/`yes` turn serving on but do not conjure a build — with no `apps/web/dist` the core warns and serves nothing, so run `bun run build:web` first. `0`/`false`/`no` never serve; unset serves only when `apps/web/dist/index.html` already exists. See [reverse-proxy.md](reverse-proxy.md).                                                | (auto)                                            |
| `TAU_DEMO_REVIEWER_ACCESS`                 | Serve the `/demo` app-store reviewer access page and its admin routes on a designated demo instance (`1`/`true`/`yes`). Pair with the `DEMO_REVIEWER_SECRET` secret and `tau demo seed`; see [demo-access.md](demo-access.md). | (off)                                             |
| `DEMO_REVIEWER_SECRET`                     | Reviewer access code for `/demo` (secret store; at least 16 characters). Rotate or delete to stop new reviewer pairings.                                                                                                     | (unset)                                           |
| `TAU_K8S_LOCAL`                            | Enable local K8s dev mode (k3d). Uses `kubectl port-forward` to bridge host → pod networking. Like every `TAU_K8S_*` key it applies **only** under `TAU_SANDBOX_RUNTIME=k8s`; under any other runtime it is ignored (the api/worker log one warning naming the ignored keys at boot).                                                                                                                                                                | -                                                 |
| `TAU_K8S_NAMESPACE`                        | K8s namespace for sandbox pods                                                                                                                                                                                                                                                                                                                                                                                                                       | `tau-sandboxes`                                   |
| `TAU_K8S_RUNTIME_CLASS`                    | K8s RuntimeClass for sandbox pods (e.g., `sysbox-runc`). Set empty to disable.                                                                                                                                                                                                                                                                                                                                                                       | `sysbox-runc`                                     |
| `GITHUB_TOKEN`                             | Retired for agent GitHub access. Reconnect through Settings → Integrations → GitHub and authorize repository access; do not configure new tokens here.                                                                                                                                                                                                                                                                                               | -                                                 |
| `LINEAR_WEBHOOK_SECRET`                    | Secret for Linear webhook signature verification                                                                                                                                                                                                                                                                                                                                                                                                     | -                                                 |
| `LINEAR_USER_ID`                           | Linear user UUID to monitor for issue assignments                                                                                                                                                                                                                                                                                                                                                                                                    | -                                                 |
| `DISCORD_*`, `SLACK_*`, `TELEGRAM_*`         | Legacy chat-bot keys. Bots are now configured as connections on their Integrations card (see [channels.md](channels.md)); a complete legacy set is migrated on first boot and kept as a fallback for one release. |

Runtime-specific variables (`vm`, `k8s`, `docker-*`) are listed per runtime in
[sandbox-runtimes.md](sandbox-runtimes.md); the full Kubernetes set is in
[k8s/deployment.md](k8s/deployment.md#appendix-a-environment-variable-reference).
Channel variables are explained in [channels.md](channels.md#environment-variables).

## Authentication

Tau uses accounts and passkeys, with user permissions, browser sessions, and revocable CLI/mobile device tokens. `bun run setup` writes a random `TAU_PASSWORD` for first-admin bootstrap. Sign in with that password and register the first administrator's passkey; once an admin holds a passkey, password login and password bearer authentication are rejected.

A bare local installation without `TAU_PASSWORD` allows first-user registration without the bootstrap-password gate. It does not make protected API routes public, and clearing the variable does not disable established account authentication. Password mode remains available if admin records exist but no admin has a passkey, for example after a restore that removed credentials.

Browser login sets an HttpOnly session cookie. Browser WebSockets use a short-lived, single-use ticket. CLI and other API clients send bearer credentials. Webhook ingress uses provider signature verification; authenticated management endpoints still require permissions. See [core-auth.md](core-auth.md) for the complete model and [docs/wiki/setup.md](setup.md#first-admin) for first-run instructions.

### CLI authentication

After registering your account, authorize a revocable CLI device token in the browser:

```bash
tau auth login local --api-url http://localhost:3000
tau auth status
tau auth introspect
```

Login stores the backend and its device credential locally; **Settings → Paired Devices** can revoke it. `tau auth logout local` revokes the device token and removes the local backend. `--local-only` removes local credentials without server revocation.

The CLI still supports an explicitly supplied bearer credential and bootstrap `TAU_PASSWORD` from the environment or a `.env` found by walking up from the current directory. Those password paths only authenticate while no admin has a passkey. An explicit `--password` or shell `TAU_PASSWORD` selects credential-based login instead of browser authorization; clear a stale shell password before using browser login. Use `tau auth status` to inspect the active credential source.

## Integration credentials and optional services

Connect GitHub through **Settings → Integrations → GitHub**, install the GitHub App on the required repositories, and select or inherit the account in the squad's **Integrations** settings. Account connection alone does not grant repository access. Legacy `GITHUB_USER`, `GITHUB_TOKEN`, `GH_TOKEN`, and per-squad token-secret settings are retired; reconnect historical configurations. Git author name/email overrides remain separate under **Settings → Git**. See [GitHub account connections](github-integrations.md).

Agent-model accounts belong to **AI Providers**. **Integrations → OpenAI API services** supplies Realtime voice, audio transcription, and embeddings independently of ChatGPT/model accounts. **Assistant & Memory** controls the Voice assistant and Semantic memory search features and exposes the same API-service connection. For services-only setup, use the integration card rather than exporting `OPENAI_API_KEY`, which retains legacy model-provider discovery behavior.

**Integrations → Google Cloud** supplies message read-aloud through Google Text-to-Speech. Configure service-account JSON there, or set the server's `GOOGLE_APPLICATION_CREDENTIALS` path for Application Default Credentials; the integration must be enabled in either case. This is separate from the Assistant's OpenAI Realtime conversation. API-service and Google speech credential changes apply to new requests without a restart.

## Configuration as code

Everything under `config/` (agent types, squad presets, skills, channels,
notification rules, webhook actions) is YAML synced into the database on
startup — see [README.md](README.md#configuration-as-code) and
[config-sync.md](config-sync.md).
