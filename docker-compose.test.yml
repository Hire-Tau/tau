services:
  postgres:
    image: paradedb/paradedb:latest
    # Sweep metadata: apps/core/src/test-db-sweep.ts reaps projects whose
    # repo-root no longer exists (deleted worktrees). TEST_REPO_ROOT is set by
    # test-setup.ts / test-db.ts at `up` time.
    labels:
      dev.tau.test-db: 'true'
      dev.tau.test-db.repo-root: '${TEST_REPO_ROOT:-}'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tau_test
    ports:
      - '${TEST_DB_PORT:-5433}:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d tau_test']
      interval: 2s
      timeout: 5s
      retries: 10
    volumes:
      - ./docker/init-test-db.sql:/docker-entrypoint-initdb.d/init-test-db.sql:ro
    tmpfs:
      - /var/lib/postgresql
