# Rendered by scripts/setup/setup-host.sh — do not edit the installed copy by
# hand; re-run setup-host.sh (or edit this template) instead.
[Unit]
Description=Nightly trigger for tau-backup.service

[Timer]
OnCalendar=@ONCALENDAR@
# Catch up on the next boot if the host was off at the scheduled time.
Persistent=true
Unit=tau-backup.service

[Install]
WantedBy=timers.target
