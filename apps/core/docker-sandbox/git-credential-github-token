#!/bin/sh
# Git credential helper that returns GITHUB_TOKEN for github.com HTTPS URLs.
# Reads the token from the environment at runtime.
if [ -n "$GITHUB_TOKEN" ]; then
  echo "protocol=https"
  echo "host=github.com"
  echo "username=x-access-token"
  echo "password=$GITHUB_TOKEN"
fi
