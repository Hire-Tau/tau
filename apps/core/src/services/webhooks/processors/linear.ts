// Compatibility entry point for the shared webhook registry. Provider behavior lives in integrations.
export { linearProcessor, handleLinearIssueUpdate, setLinearActionConfig } from '../../integrations/linear/webhook'
